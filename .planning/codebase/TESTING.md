# Testing

## Current State
- No formal testing framework (e.g., Jest, Vitest, Playwright, Cypress) is currently set up in the `package.json`.

## Guidelines (When Added)
- Unit tests typically co-locate with the component or utility.
- E2E tests would reside in a separate root-level folder.
